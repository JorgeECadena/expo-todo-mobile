import { View, Text } from "react-native";

const Create = () => {
    return (
        <View className="flex-1 justify-center items-center">
            <Text className="text-red-700">Create atelier</Text>
        </View>
    );
};

export default Create;